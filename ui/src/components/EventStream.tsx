/**
 * Given one or more event queries, this component will connect to the selected network
 * and monitor events that match provided queries. It will then display the events in a
 * stream, with the most recent events at the top.
 */

import { Flex, Box, Heading } from '@chakra-ui/react'

function EventStream() {
  return (
    <Box>
      EventStream
    </Box>
  )
}

export default EventStream